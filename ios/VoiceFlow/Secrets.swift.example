//
//  Secrets.swift.example
//  VoiceFlow
//
//  Copy this file to `Secrets.swift` (same directory) and drop in your
//  real Anthropic API key. The real Secrets.swift is gitignored.
//
//  Get a key at: https://console.anthropic.com/
//

import Foundation

enum Secrets {
    /// Anthropic API key used by DictationEngine -> AnthropicClient.
    /// Treat this like a password; never commit the populated file.
    static let anthropicAPIKey = "sk-ant-REPLACE-ME"
}
